/**
 * Function to convert string to lowercase
 * @param {String} str the input string argument
 * @return {String} the input string in lowercase
 */
function toLower(str) {
  return str.toLowerCase();
}

module.exports = {
  toLower,
};
